using BL.BS.Two.UI.Tito;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
namespace BL.BS.Two.UI.Icon
{
    public class IconItem : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            //transform.Find("Tito").GetComponent<TitoItem>().SetColorAndAnimation(new Color(1, 0, 0));
        }
        // Update is called once per frame
        void Update()
        {

        }
        public Transform targetItem;//目标游戏物体
    }
}

